* == 💡utility routines -- for -- manipulating filename paths💡 /
  * compatible with the target OS-defined file paths
    * == 👀use -- , based on the OS, -- `/` OR `\`👀

* ❌NOT good use case❌
  * `/` INDEPENDENT of OS
    * _Example:_ URLs
    * -> use [path](../) package